---
import "../styles/global.css";

interface Props {
  title?: string;
  description?: string;
}

const {
  title = "LaunchLoom — websites that move fast",
  description = "A focused website production system for local businesses.",
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#10251f" />
    <title>{title}</title>
  </head>
  <body>
    <slot />
  </body>
</html>
