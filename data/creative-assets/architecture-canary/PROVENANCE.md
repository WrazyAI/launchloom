# Architecture canary image fixtures

These three WebP files are local generated-image fixtures for deterministic
creative-canary rendering only. They are not source-site reference screenshots,
client-provided media, or production-ready images.

They were previously stored under `data/inspiration-evidence/creative-probe-kokoro/assets/`
and have been moved here because the current model-authored canary targets the
canonical architecture niche. The original generation prompt and provider
receipt are not present in the repository, so do not claim a specific model or
prompt for these files. Production image generation continues to use the
per-intake asset pipeline and its normal provenance manifest.
